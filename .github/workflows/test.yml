name: Execute Tests
on: [push]
jobs:
  easytest:
    name: Run tests for easytest
    runs-on: ubuntu-latest
    steps:
      - uses: ./.github/workflows/testTemplate.yml
        with:
          project: "easytest"
